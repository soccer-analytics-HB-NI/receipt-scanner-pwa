// config.example.js - Copy this to config.js and add your actual API key
window.APP_CONFIG = {
    // Get your API key from: https://platform.openai.com/api-keys
    OPENAI_API_KEY: 'sk-proj-your-api-key-here',
    
    // Default AI setting (true = use AI, false = basic parsing)
    USE_AI_BY_DEFAULT: true,
    
    // App settings
    MAX_FILE_SIZE_MB: 25,
    OCR_TIMEOUT_SECONDS: 90
};
